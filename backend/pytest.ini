[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Show short traceback for faster iteration
addopts = -v --tb=short
markers =
    llm: tests that call OpenAI API (deselect with -m "not llm")
